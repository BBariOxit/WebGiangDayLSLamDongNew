############################################
# MSSQL
# Production: tạo user riêng thay vì dùng sa
# Bạn đang dùng sa với mật khẩu 000000 (KHÔNG an toàn) => chỉ dùng local dev.
############################################
DB_SERVER=localhost
DB_DATABASE=HistoryEduDB
DB_USER=sa        # hoặc app_user
DB_PASSWORD=000000
DB_ENCRYPT=false
DB_TRUST_SERVER_CERT=true

# AUTH
JWT_ACCESS_SECRET=change_access_secret
JWT_REFRESH_SECRET=change_refresh_secret
JWT_ACCESS_EXPIRES=15m
JWT_REFRESH_EXPIRES=30d
PASSWORD_SALT_ROUNDS=10

# GOOGLE OAUTH (ID TOKEN VERIFY)
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com

# SERVER
PORT=4000
CORS_ORIGIN=http://localhost:5173

# Gợi ý: khi deploy đổi tất cả secret và dùng tài khoản DB riêng ít quyền.
